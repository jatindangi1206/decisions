# Tested stack. The RL libs are the version-sensitive ones (env/maze/render API), so they are
# pinned; the numerics libs are floored. On a Linux CUDA box, `pip install torch` pulls the CUDA
# wheel by default — that is fine.
torch>=2.2
numpy<2                    # torch 2.2.x requires numpy<2; relax if you move to a numpy-2 torch build
scipy>=1.10
matplotlib>=3.7
PyYAML>=6.0
minari==0.5.3
gymnasium==1.3.0
gymnasium-robotics==1.4.2
mujoco==3.10.0
imageio>=2.30
